const NewIndex = () => {
  return (
    <div className="min-h-screen bg-background flex items-center justify-center">
      <p className="text-muted-foreground">Nowa strona główna — treść wkrótce</p>
    </div>
  );
};

export default NewIndex;
